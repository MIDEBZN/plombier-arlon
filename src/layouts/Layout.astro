---
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import MobileStickyCall from '../components/MobileStickyCall.astro';
import WhatsAppChatWidget from '../components/WhatsAppChatWidget.astro';
import GoogleMapEmbed from '../components/GoogleMapEmbed.astro';
import '../styles/global.css';

export interface Props {
  title: string;
  description: string;
  canonicalPath?: string;
  schemaType?: string;
}

const { 
  title, 
  description, 
  canonicalPath = '',
  schemaType = 'Plumber'
} = Astro.props;

const siteUrl = 'https://plombier-arlon.be';
const canonicalUrl = `${siteUrl}${canonicalPath ? canonicalPath : Astro.url.pathname}`;

// Complete local business schema
const jsonLd = {
  "@context": "https://schema.org",
  "@type": "Plumber",
  "@id": `${siteUrl}/#organization`,
  "name": "Plombier Arlon",
  "legalName": "Plombier Arlon SRL",
  "url": siteUrl,
  "telephone": "+32465286577",
  "email": "contact@plombierarlon.be",
  "priceRange": "€€",
  "image": `${siteUrl}/images/plombier-arlon-camionnette.webp`,
  "logo": `${siteUrl}/images/logo.png`,
  "address": {
    "@type": "PostalAddress",
    "streetAddress": "Chem. de Weyler 48",
    "addressLocality": "Arlon",
    "postalCode": "6700",
    "addressRegion": "Province de Luxembourg",
    "addressCountry": "BE"
  },
  "geo": {
    "@type": "GeoCoordinates",
    "latitude": 49.6712,
    "longitude": 5.8134
  },
  "openingHoursSpecification": [
    {
      "@type": "OpeningHoursSpecification",
      "dayOfWeek": ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday", "Sunday"],
      "opens": "00:00",
      "closes": "23:59"
    }
  ],
  "areaServed": [
    { "@type": "City", "name": "Arlon" },
    { "@type": "AdministrativeArea", "name": "Bonnert" },
    { "@type": "AdministrativeArea", "name": "Weyler" },
    { "@type": "AdministrativeArea", "name": "Stockem" },
    { "@type": "AdministrativeArea", "name": "Messancy" },
    { "@type": "AdministrativeArea", "name": "Aubange" },
    { "@type": "AdministrativeArea", "name": "Habay" },
    { "@type": "AdministrativeArea", "name": "Attert" },
    { "@type": "City", "name": "Steinfort" },
    { "@type": "City", "name": "Capellen" }
  ],
  "aggregateRating": {
    "@type": "AggregateRating",
    "ratingValue": "4.9",
    "reviewCount": "184",
    "bestRating": "5"
  }
};
---

<!DOCTYPE html>
<html lang="fr-BE" class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
    <meta name="theme-color" content="#001f43" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonicalUrl} />
    <meta name="robots" content="index, follow, max-image-preview:large, max-snippet:-1" />

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonicalUrl} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:locale" content="fr_BE" />
    <meta property="og:site_name" content="Plombier Arlon" />
    <meta property="og:image" content={`${siteUrl}/images/og-plombier-arlon.webp`} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />

    <!-- Twitter Cards -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={`${siteUrl}/images/og-plombier-arlon.webp`} />

    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />

    <!-- Preconnect to Font Origins -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />

    <!-- Optimized Font Loading: display=swap, non-blocking asynchronous load -->
    <link rel="preload" as="style" href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200&display=swap" />
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200&display=swap" media="print" onload="this.media='all'" />
    <noscript>
      <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200&display=swap" />
    </noscript>

    <!-- Critical Above-Fold CSS (Inlined for instant FCP & LCP Core Web Vitals) -->
    <style is:inline>
      :root {
        --color-primary: #001f43;
        --color-secondary: #bb0027;
        --color-surface: #f9f9ff;
        --color-text: #121b2e;
      }
      *, *::before, *::after { box-sizing: border-box; }
      html {
        scroll-behavior: smooth;
        -webkit-text-size-adjust: 100%;
        font-family: 'Plus Jakarta Sans', system-ui, -apple-system, sans-serif;
      }
      body {
        margin: 0;
        background-color: var(--color-surface);
        color: var(--color-text);
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        overflow-x: hidden;
      }
      header {
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        z-index: 40;
        background-color: #ffffff;
      }
      main {
        flex-grow: 1;
        width: 100%;
        padding-top: 5rem;
      }
      @media (min-width: 640px) {
        main { padding-top: 7rem; }
      }
      .bg-primary { background-color: var(--color-primary); }
      .bg-secondary { background-color: var(--color-secondary); }
      .text-primary { color: var(--color-primary); }
      .text-secondary { color: var(--color-secondary); }
    </style>

    <!-- JSON-LD Structured Data -->
    <script type="application/ld+json" set:html={JSON.stringify(jsonLd)} />
  </head>

  <body class="bg-background font-sans text-on-surface antialiased selection:bg-secondary-fixed selection:text-on-secondary-fixed min-h-screen flex flex-col">
    <Header />
    
    <!-- Main content with top offset for fixed header -->
    <main class="w-full flex-grow pt-20 sm:pt-28">
      <slot />
    </main>

    <!-- Embedded Google Map on all pages -->
    <GoogleMapEmbed />

    <Footer />
    <MobileStickyCall />
    <WhatsAppChatWidget />
  </body>
</html>
